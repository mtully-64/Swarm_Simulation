# Swarm Simulation

## Description of the Project

This project builds a multi-agent system with emergent behaviour, where a large number of independent agents (e.g. dots, particles) move around the same environment at the same time.

Each agent follows a small set of simple, local rules that are based on what it can only "see" in its immediate surroundings, rather than having a global knowledge of the system or a central controller.